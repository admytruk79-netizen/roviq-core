import {useEffect,useState,type FormEvent} from 'react';type Principal={role:string;actorId?:string|null};type Order={id:string;case_id:string;status:string;needed_by?:string;created_at?:string};const BASE=(import.meta.env.VITE_API_BASE_URL??'').replace(/\/$/,'');const TOKEN='roviq_parts_token',PRINCIPAL='roviq_parts_principal';async function req<T>(path:string,init:RequestInit={}):Promise<T>{const t=localStorage.getItem(TOKEN);const r=await fetch(`${BASE}${path}`,{...init,headers:{'content-type':'application/json',...(t?{authorization:`Bearer ${t}`}:{})}});if(!r.ok){const d=await r.json().catch(()=>({}));throw new Error(d.error??`request_failed_${r.status}`)}return r.json()}export default function App(){const[p,setP]=useState<Principal|null>(()=>{try{return JSON.parse(localStorage.getItem(PRINCIPAL)??'null')}catch{return null}}),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[error,setError]=useState(''),[orders,setOrders]=useState<Order[]>([]);async function login(e:FormEvent){e.preventDefault();try{setError('');const r=await req<{accessToken:string;principal:Principal}>('/api/auth/login',{method:'POST',body:JSON.stringify({email:email.trim(),password})});if(r.principal.role!=='parts')throw new Error('This account is not a parts-vendor account.');localStorage.setItem(TOKEN,r.accessToken);localStorage.setItem(PRINCIPAL,JSON.stringify(r.principal));setP(r.principal)}catch(e){setError(e instanceof Error?e.message:'Unable to sign in')}}async function load(){try{const r=await req<{orders:Order[]}>('/api/parts/me/orders');setOrders(r.orders)}catch(e){setError(e instanceof Error?e.message:'Unable to load orders')}}useEffect(()=>{if(p)void load()},[p]);async function action(id:string,kind:string){try{setError('');if(kind==='reserve')await req(`/api/parts/orders/${id}/reserve`,{method:'POST'});else await req(`/api/parts/orders/${id}/status`,{method:'POST',body:JSON.stringify({status:kind})});await load()}catch(e){setError(e instanceof Error?e.message:'Update failed')}}function logout(){localStorage.clear();setP(null)}if(!p)return <Login {...{email,setEmail,password,setPassword,show,setShow,error,login}}/>;return <div className="shell"><Header logout={logout}/><main><span className="eyebrow">Fulfilment</span><h1>Parts orders</h1><p>Confirm inventory, reserve stock and keep fulfilment status current.</p>{error&&<div className="error">{error}</div>}<div className="grid">{orders.map(o=><div className="card" key={o.id}><span className="pill">{o.status}</span><h3>Order {o.id.slice(0,8)}</h3><p>Case {o.case_id.slice(0,8)}</p><div className="actions">{o.status==='assigned'&&<button className="primary" onClick={()=>action(o.id,'reserve')}>Reserve</button>}{o.status==='reserved'&&<button className="primary" onClick={()=>action(o.id,'ordered')}>Mark ordered</button>}{o.status==='ordered'&&<button className="primary" onClick={()=>action(o.id,'shipped')}>Mark shipped</button>}{o.status==='shipped'&&<button className="primary" onClick={()=>action(o.id,'delivered')}>Mark delivered</button>}</div></div>)}</div></main></div>}function Login(p:any){return <div className="shell login"><form className="panel login-card" onSubmit={p.login}><Brand/><span className="eyebrow">Parts vendor portal</span><h1>Sign in</h1><p>Confirm, fulfil and deliver without losing the service context.</p><label>Email<input type="email" required autoComplete="email" value={p.email} onChange={(e:any)=>p.setEmail(e.target.value)}/></label><label>Password<div className="password"><input type={p.show?'text':'password'} required autoComplete="current-password" value={p.password} onChange={(e:any)=>p.setPassword(e.target.value)}/><button type="button" onClick={()=>p.setShow(!p.show)}>{p.show?'Hide':'Show'}</button></div></label>{p.error&&<div className="error">{p.error}</div>}<button className="primary">Enter portal</button></form></div>}function Brand(){return <div className="brand"><span className="mark">R</span><span>ROVIQ</span></div>}function Header({logout}:any){return <header><Brand/><div><span>Parts</span><button className="secondary" onClick={logout}>Sign out</button></div></header>}
