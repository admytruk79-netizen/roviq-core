const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}});
let neonLoader;
async function sqlFor(env){if(!env.DATABASE_URL)throw new Error('database_not_configured');neonLoader??=import('@neondatabase/serverless').then(m=>m.neon);const neon=await neonLoader;return neon(env.DATABASE_URL);}
function authorized(req,env){const k=req.headers.get('x-admin-api-key');return Boolean(env.ADMIN_API_KEY&&k===env.ADMIN_API_KEY);}
function safety(symptoms='',observations={}){const t=`${symptoms} ${JSON.stringify(observations)}`.toLowerCase();const rules=[['fire_or_smoke',/\b(fire|flames|smoke from engine|burning smell)\b/],['fuel_leak',/\b(fuel leak|gasoline leak|strong fuel smell|smells like gas)\b/],['brake_failure',/\b(no brakes|brake failure|brake pedal.*floor)\b/],['steering_failure',/\b(steering locked|cannot steer|steering failure)\b/],['oil_pressure',/\b(oil pressure warning|low oil pressure)\b/],['overheating',/\b(overheating|temperature gauge.*red|coolant.*boiling)\b/],['ev_high_voltage',/\b(high voltage warning|battery fire|thermal runaway)\b/],['severe_misfire',/\b(flashing check engine|engine.*shaking violently|severe misfire)\b/]];const flags=rules.filter(([,r])=>r.test(t)).map(([code])=>({code,severity:'critical',rationale:'Deterministic ROVIQ safety rule matched reported symptoms.'}));return{flags,forceNonDrivable:flags.length>0};}
function normalize(x={},symptoms=''){const d=['unknown','drivable','limited','non_drivable'];return{symptomSummary:String(x.symptomSummary||symptoms||'Vehicle concern reported').slice(0,1000),suggestedCapabilities:Array.isArray(x.suggestedCapabilities)?x.suggestedCapabilities.map(String).slice(0,8):['diagnostics'],suggestedDrivability:d.includes(x.suggestedDrivability)?x.suggestedDrivability:'unknown',safetyFlags:Array.isArray(x.safetyFlags)?x.safetyFlags.slice(0,12):[],evidence:Array.isArray(x.evidence)?x.evidence.slice(0,12):[],confidence:Number.isFinite(Number(x.confidence))?Math.max(0,Math.min(1,Number(x.confidence))):0.2,missingInformation:Array.isArray(x.missingInformation)?x.missingInformation.map(String).slice(0,12):[],suggestedActions:Array.isArray(x.suggestedActions)?x.suggestedActions.slice(0,8):[]};}
const schema={type:'object',additionalProperties:false,properties:{symptomSummary:{type:'string'},suggestedCapabilities:{type:'array',items:{type:'string'}},suggestedDrivability:{type:'string',enum:['unknown','drivable','limited','non_drivable']},safetyFlags:{type:'array',items:{type:'object',properties:{code:{type:'string'},severity:{type:'string'},rationale:{type:'string'}},required:['code','severity','rationale']}},evidence:{type:'array',items:{type:'object',properties:{source:{type:'string'},statement:{type:'string'}},required:['source','statement']}},confidence:{type:'number'},missingInformation:{type:'array',items:{type:'string'}},suggestedActions:{type:'array',items:{type:'object',properties:{actionType:{type:'string'},actionPayload:{type:'object'}},required:['actionType']}}},required:['symptomSummary','suggestedCapabilities','suggestedDrivability','safetyFlags','evidence','confidence','missingInformation','suggestedActions']};
async function runTriage(env,body){const{caseId,symptoms,vehicle={},observations={}}=body||{};if(!caseId||!symptoms)return{error:'caseId_and_symptoms_required',status:400};const sql=await sqlFor(env);const cases=await sql`select id from service_cases where id=${caseId} limit 1`;if(!cases.length)return{error:'service_case_not_found',status:404};let raw;try{const r=await env.AI.run(env.TRIAGE_MODEL||'@cf/meta/llama-3.3-70b-instruct-fp8-fast',{messages:[{role:'system',content:'You are ROVIQ automotive triage. Be conservative. Never select providers or claim a definitive diagnosis. Return only structured JSON for safety, drivability, missing information, and required capabilities.'},{role:'user',content:JSON.stringify({symptoms,vehicle,observations})}],response_format:{type:'json_schema',json_schema:schema},temperature:0});raw=r?.response??r;if(typeof raw==='string')raw=JSON.parse(raw);}catch(e){raw={symptomSummary:symptoms,suggestedCapabilities:['diagnostics'],suggestedDrivability:'unknown',safetyFlags:[],evidence:[],confidence:0.2,missingInformation:['Professional diagnostic assessment required'],suggestedActions:[{actionType:'request_diagnostic_review'}]};}
let result=normalize(raw,symptoms);const s=safety(symptoms,observations);if(s.forceNonDrivable){result.suggestedDrivability='non_drivable';result.suggestedCapabilities=[...new Set([...result.suggestedCapabilities,'diagnostics','tow'])];}result.safetyFlags=[...s.flags,...result.safetyFlags];const review=s.forceNonDrivable||result.confidence<Number(env.TRIAGE_AUTO_CONFIDENCE_THRESHOLD||0.9)||result.safetyFlags.some(x=>x.severity==='critical');const mode=env.TRIAGE_DEPLOYMENT_MODE||'shadow';const rows=await sql`insert into ai_triage_assessments(case_id,source,model_provider,model_name,input_snapshot,symptom_summary,suggested_capabilities,suggested_drivability,safety_flags,evidence,confidence,requires_human_review,status) values(${caseId},'ai_engine','cloudflare-workers-ai',${env.TRIAGE_MODEL||'@cf/meta/llama-3.3-70b-instruct-fp8-fast'},${JSON.stringify({symptoms,vehicle,observations,mode})}::jsonb,${result.symptomSummary},${JSON.stringify(result.suggestedCapabilities)}::jsonb,${result.suggestedDrivability},${JSON.stringify(result.safetyFlags)}::jsonb,${JSON.stringify(result.evidence)}::jsonb,${result.confidence},${review},'proposed') returning id,created_at`;return{status:200,assessmentId:rows[0].id,mode,safetyOverride:s.forceNonDrivable,requiresHumanReview:review,result,createdAt:rows[0].created_at};}
export default{async fetch(request,env){const url=new URL(request.url);try{if(url.pathname==='/'||url.pathname==='/health'||url.pathname==='/edge-health')return json({ok:true,service:'roviq-core',runtime:'cloudflare-worker',triage:'native-shadow'});if(url.pathname==='/ready'){const sql=await sqlFor(env);const rows=await sql`select now() as database_time`;return json({ok:true,service:'roviq-core',database:'reachable',databaseTime:rows[0]?.database_time??null,aiBinding:Boolean(env.AI)});}if(url.pathname==='/api/triage/run'&&request.method==='POST'){if(!authorized(request,env))return json({error:'unauthorized'},401);if(!env.AI)return json({error:'workers_ai_not_bound'},503);const out=await runTriage(env,await request.json());return out.error?json({error:out.error},out.status):json(out);}return json({ok:true,service:'roviq-core',runtime:'cloudflare-worker',database:'neon',aiTriage:{status:'shadow',endpoint:'/api/triage/run'}});}catch(e){return json({ok:false,error:'worker_runtime_error',detail:String(e?.message||e)},500);}}};
