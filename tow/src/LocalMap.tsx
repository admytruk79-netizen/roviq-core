import {useEffect,useRef,useState} from 'react';
type Point={lat:number;lng:number};type LivePoint=Point&{heading:number|null;accuracy:number|null;speed:number|null};type Spatial={origin?:unknown;current_vehicle?:unknown;destination?:unknown;transport_location?:unknown;route_context?:Record<string,unknown>};type DispatchRoute={pickup_location?:unknown;dropoff_location?:unknown};type RouteStep={instruction?:string;type?:string;name?:string};type Route={geometry?:{coordinates?:number[][]};distance?:number;duration?:number;steps?:RouteStep[]};type Props={caseId?:string;dispatchId?:string;dispatchStatus?:string};type MapMode='day'|'twilight'|'night';
const BASE=(import.meta.env.VITE_API_BASE_URL??'').replace(/\/$/,'');const LOCAL='https://roviq-local2.admytruk79.workers.dev';const TOKEN='roviq_tow_token';const MODE_KEY='roviq_tow_map_mode';const PICKUP_STATES=new Set(['assigned','accepted','en_route','arrived']);
function auth():Record<string,string>{const t=localStorage.getItem(TOKEN);return t?{authorization:`Bearer ${t}`}:{}}
function valid(lat:number,lng:number){return Number.isFinite(lat)&&Number.isFinite(lng)&&Math.abs(lat)<=90&&Math.abs(lng)<=180}
function point(value:unknown,depth=0):Point|null{if(value==null||depth>4)return null;if(Array.isArray(value)&&value.length>=2){const lng=Number(value[0]),lat=Number(value[1]);return valid(lat,lng)?{lat,lng}:null}if(typeof value==='string'){const m=value.trim().match(/^(-?\d+(?:\.\d+)?)\s*,\s*(-?\d+(?:\.\d+)?)$/);if(m){const lat=Number(m[1]),lng=Number(m[2]);if(valid(lat,lng))return{lat,lng}}return null}if(typeof value!=='object')return null;const v=value as Record<string,unknown>,lat=Number(v.lat??v.latitude),lng=Number(v.lng??v.lon??v.longitude);if(valid(lat,lng))return{lat,lng};if(Array.isArray(v.coordinates)){const p=point(v.coordinates,depth+1);if(p)return p}for(const k of ['location','point','position','geometry','coords','value']){const p=point(v[k],depth+1);if(p)return p}return null}
function label(value:unknown){if(!value)return'Not available';if(typeof value==='string')return value;if(typeof value==='object'){const v=value as Record<string,unknown>;const s=[v.name,v.label,v.address,v.formatted_address,v.description].find(x=>typeof x==='string'&&x.trim());if(typeof s==='string')return s;const p=point(v);if(p)return`${p.lat.toFixed(5)}, ${p.lng.toFixed(5)}`}return'Not available'}
function rad(n:number){return n*Math.PI/180}function distanceM(a:Point,b:Point){const R=6371000,dLat=rad(b.lat-a.lat),dLng=rad(b.lng-a.lng),q=Math.sin(dLat/2)**2+Math.cos(rad(a.lat))*Math.cos(rad(b.lat))*Math.sin(dLng/2)**2;return 2*R*Math.asin(Math.min(1,Math.sqrt(q)))}
function autoMode():MapMode{const h=new Date().getHours();if(h>=7&&h<18)return'day';if((h>=18&&h<20)||(h>=5&&h<7))return'twilight';return'night'}function savedMode():MapMode{try{const m=localStorage.getItem(MODE_KEY);if(m==='day'||m==='twilight'||m==='night')return m}catch{}return autoMode()}function nextMode(m:MapMode):MapMode{return m==='day'?'twilight':m==='twilight'?'night':'day'}function modeIcon(m:MapMode){return m==='day'?'☀':m==='twilight'?'◐':'☾'}function frameFilter(m:MapMode){if(m==='twilight')return'brightness(.72) saturate(.82) sepia(.16) contrast(1.08)';if(m==='night')return'brightness(.34) saturate(.68) contrast(1.24) sepia(.08)';return'none'}function stepText(r:Route|null){const s=r?.steps?.find(x=>x.instruction)||r?.steps?.[0];return s?.instruction??(s?.type==='arrive'?'Arrive at destination':s?.name?`Continue on ${s.name}`:'')}
export function LocalMap({caseId,dispatchId,dispatchStatus}:Props){const[mode,setMode]=useState<MapMode>(savedMode),assigned=Boolean(caseId&&dispatchId);const[spatial,setSpatial]=useState<Spatial|null>(null),[dispatch,setDispatch]=useState<DispatchRoute|null>(null),[live,setLive]=useState<LivePoint|null>(null),[route,setRoute]=useState<Route|null>(null),[tracking,setTracking]=useState<'starting'|'live'|'limited'>('starting'),[mapReady,setMapReady]=useState(false);const frameRef=useRef<HTMLIFrameElement|null>(null),lastPush=useRef(0),lastRouteAt=useRef(0),lastRouted=useRef<Point|null>(null),lastTarget=useRef('');
useEffect(()=>{try{localStorage.setItem(MODE_KEY,mode)}catch{}},[mode]);
useEffect(()=>{if(!caseId){setSpatial(null);return}let dead=false;const load=async()=>{try{const r=await fetch(`${BASE}/api/maintenance/cases/${caseId}/spatial`,{headers:auth(),cache:'no-store'});if(!r.ok)throw 0;const d=await r.json() as{spatial:Spatial};if(!dead)setSpatial(d.spatial??null)}catch{if(!dead)setSpatial(null)}};void load();const id=setInterval(()=>void load(),10000);return()=>{dead=true;clearInterval(id)}},[caseId]);
useEffect(()=>{if(!dispatchId){setDispatch(null);return}let dead=false;const load=async()=>{try{const r=await fetch(`${BASE}/api/transport/${dispatchId}`,{headers:auth(),cache:'no-store'});if(!r.ok)throw 0;const d=await r.json() as{dispatch:DispatchRoute};if(!dead)setDispatch(d.dispatch)}catch{if(!dead)setDispatch(null)}};void load();const id=setInterval(()=>void load(),10000);return()=>{dead=true;clearInterval(id)}},[dispatchId]);
useEffect(()=>{if(!navigator.geolocation){setTracking('limited');return}const id=navigator.geolocation.watchPosition(pos=>{const p:LivePoint={lat:pos.coords.latitude,lng:pos.coords.longitude,heading:Number.isFinite(pos.coords.heading)?pos.coords.heading:null,accuracy:Number.isFinite(pos.coords.accuracy)?pos.coords.accuracy:null,speed:Number.isFinite(pos.coords.speed)?pos.coords.speed:null};setLive(p);setTracking('live');const now=Date.now();if(dispatchId&&now-lastPush.current>8000){lastPush.current=now;void fetch(`${BASE}/api/transport/${dispatchId}/location`,{method:'POST',headers:{'content-type':'application/json',...auth()},body:JSON.stringify({...p,capturedAt:new Date(pos.timestamp).toISOString()})}).catch(()=>{})}},()=>setTracking('limited'),{enableHighAccuracy:true,maximumAge:1000,timeout:12000});return()=>navigator.geolocation.clearWatch(id)},[dispatchId]);
function pickup(){return point(dispatch?.pickup_location)??point(spatial?.origin)??point(spatial?.current_vehicle)}function destination(){return point(dispatch?.dropoff_location)??point(spatial?.destination)}function target(){return dispatchStatus&&PICKUP_STATES.has(dispatchStatus)?pickup():destination()}
async function requestRoute(from:Point,to:Point){const key=`${to.lng.toFixed(5)},${to.lat.toFixed(5)}`,moved=lastRouted.current?distanceM(from,lastRouted.current):Infinity;if(key===lastTarget.current&&Date.now()-lastRouteAt.current<10000&&moved<45)return;lastTarget.current=key;lastRouteAt.current=Date.now();lastRouted.current={...from};try{const r=await fetch(`${BASE}/api/local/route?from=${encodeURIComponent(`${from.lng},${from.lat}`)}&to=${encodeURIComponent(`${to.lng},${to.lat}`)}`,{cache:'no-store'});if(!r.ok)throw 0;const d=await r.json();setRoute(d?.route?.geometry?.coordinates?.length?d.route:null)}catch{setRoute(null)}}
useEffect(()=>{const on=(e:MessageEvent)=>{if(e.origin===LOCAL&&e.data?.type==='roviq:tow-map-ready')setMapReady(true)};window.addEventListener('message',on);return()=>window.removeEventListener('message',on)},[]);useEffect(()=>{const tow=live??point(spatial?.transport_location),t=target();if(tow&&t)void requestRoute(tow,t);else setRoute(null)},[live,spatial,dispatch,dispatchStatus]);useEffect(()=>{if(!mapReady||!frameRef.current?.contentWindow)return;frameRef.current.contentWindow.postMessage({type:'roviq:tow-map-state',state:{tow:live??point(spatial?.transport_location),pickup:pickup(),destination:destination(),follow:true,assigned},route},LOCAL)},[mapReady,live,spatial,dispatch,route,assigned,dispatchStatus]);
const distanceMiles=typeof route?.distance==='number'?route.distance/1609.344:NaN,etaMinutes=typeof route?.duration==='number'?route.duration/60:NaN,goingToPickup=Boolean(dispatchStatus&&PICKUP_STATES.has(dispatchStatus)),missing=assigned&&(goingToPickup?!pickup():!destination()),instruction=missing?(goingToPickup?'Pickup location unavailable':'Destination not assigned'):stepText(route)||(goingToPickup?'Proceed to pickup':'Proceed to destination');
return <section className="map-panel dispatch-context"><div className="map-head"><div><span className="eyebrow">Live route</span><h2>{assigned?(goingToPickup?'To pickup':'To destination'):'Standby map'}</h2></div><span className={`live-badge ${tracking}`}>{tracking==='live'?'● GPS LIVE':tracking==='starting'?'LOCATING…':'GPS LIMITED'}</span></div><div className={`live-map-wrap rq-local-${mode}`}><iframe ref={frameRef} className="live-map-frame rq-local-tow-frame" src={`${LOCAL}/tow-map.html?mode=day`} title="ROVIQ Tow map" allow="geolocation" onLoad={()=>setMapReady(false)} style={{filter:frameFilter(mode),transition:'filter .35s ease'}}/><button type="button" className="rq-weather rq-tow-mode" onClick={()=>setMode(nextMode(mode))}><span>{modeIcon(mode)}</span><span>{mode.toUpperCase()}</span></button>{assigned&&<section className="rq-nav-hud rq-tow-hud"><div className="rq-nav-next">{instruction}</div><div className="rq-nav-meta"><strong>{Number.isFinite(etaMinutes)?`${Math.max(1,Math.round(etaMinutes))} min`:'—'}</strong><span>{Number.isFinite(distanceMiles)?`${distanceMiles.toFixed(1)} mi`:'—'}</span><span>{live?.speed!=null?`${Math.max(0,Math.round(live.speed*2.23694))} mph`:'0 mph'}</span></div></section>}<button type="button" className="rq-locate rq-map-locate" onClick={()=>frameRef.current?.contentWindow?.postMessage({type:'roviq:tow-map-state',state:{tow:live??point(spatial?.transport_location),pickup:pickup(),destination:destination(),follow:true,assigned},route},LOCAL)}>◎</button></div>{assigned&&<details className="route-details"><summary>Trip details</summary><div className="spatial-grid"><div><span>Pickup</span><strong>{label(dispatch?.pickup_location??spatial?.origin??spatial?.current_vehicle)}</strong></div><div><span>Destination</span><strong>{label(dispatch?.dropoff_location??spatial?.destination)}</strong></div><div><span>Tow position</span><strong>{label(live??spatial?.transport_location)}</strong></div></div></details>}</section>}
